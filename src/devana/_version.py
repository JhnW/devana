VERSION_MAJOR = 0  # version when they make incompatible API changes
VERSION_MINOR = 1  # version when they add functionality in a backwards-compatible manner
VERSION_MAINTENANCE = 4  # version when they make backwards-compatible bug fixes
VERSION_STRING = f"{VERSION_MAJOR}.{VERSION_MINOR}.{VERSION_MAINTENANCE}"
__version__ = VERSION_STRING
