VERSION_MAJOR = 2  # version when they make incompatible API changes
VERSION_MINOR = 2  # version when they add functionality in a backwards-compatible manner
VERSION_MAINTENANCE = 1  # version when they make backwards-compatible bug fixes
VERSION_STRING = f"{VERSION_MAJOR}.{VERSION_MINOR}.{VERSION_MAINTENANCE}"
__version__ = VERSION_STRING
